# NEONGRID — 3D Tron Light Cycles

vibecode experiment with fable 5

## Run

```sh
npm install
npm run dev
```

`npm run build` typechecks and produces `dist/`. Pushes to `main` auto-deploy to GitHub Pages via Actions.
